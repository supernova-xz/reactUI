[build]
  command = "npm run build-storybook"
  publish = "storybook-static"

# ✅ Prevent Netlify from auto-detecting Next.js
[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NETLIFY_SKIP_NEXT_PLUGIN = "true"
  NEXT_PRIVATE_SKIP_NETLIFY_PLUGIN = "true"
